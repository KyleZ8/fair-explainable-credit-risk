# Python environment
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Secrets
.env
.env.*
!.env.example

# Private project working files (owner/mentor only, not part of the public repo)
.ai/
CLAUDE.md
AGENTS.md
archive/

# Notebook checkpoints / OS cruft
.ipynb_checkpoints/
.DS_Store

# Data — the source dataset (UCI "Default of Credit Card Clients", id 350,
# CC BY 4.0, ~2.8 MB) is committed directly under data/ with attribution in
# data/README.md. Only generated/processed artifacts are ignored.
data/processed/

# Pipeline run artifacts — regenerated by `make pipeline` / notebooks.
outputs/

# Source material staging (never committed, never modified).
_source/
